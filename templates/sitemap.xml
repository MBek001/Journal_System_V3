<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">

    <!-- Homepage -->
    <url>
        <loc>{{ protocol }}://{{ domain }}/</loc>
        <changefreq>daily</changefreq>
        <priority>1.0</priority>
        <lastmod>{% now "Y-m-d" %}</lastmod>
    </url>

    <!-- Static Pages -->
    <url>
        <loc>{{ protocol }}://{{ domain }}/about/</loc>
        <changefreq>monthly</changefreq>
        <priority>0.6</priority>
    </url>

    <url>
        <loc>{{ protocol }}://{{ domain }}/contact</loc>
        <changefreq>monthly</changefreq>
        <priority>0.5</priority>
    </url>

    <url>
        <loc>{{ protocol }}://{{ domain }}/for-publishers</loc>
        <changefreq>monthly</changefreq>
        <priority>0.6</priority>
    </url>

    <url>
        <loc>{{ protocol }}://{{ domain }}/send-article</loc>
        <changefreq>monthly</changefreq>
        <priority>0.7</priority>
    </url>

    <url>
        <loc>{{ protocol }}://{{ domain }}/journals</loc>
        <changefreq>weekly</changefreq>
        <priority>0.9</priority>
    </url>

    <url>
        <loc>{{ protocol }}://{{ domain }}/articles/</loc>
        <changefreq>daily</changefreq>
        <priority>0.9</priority>
    </url>

    <url>
        <loc>{{ protocol }}://{{ domain }}/allauthors</loc>
        <changefreq>weekly</changefreq>
        <priority>0.7</priority>
    </url>

    <!-- Articles -->
    {% for article in articles %}
    <url>
        <loc>{{ protocol }}://{{ domain }}/articles/{{ article.id }}/</loc>
        <lastmod>{{ article.updated_at|date:"Y-m-d" }}</lastmod>
        <changefreq>monthly</changefreq>
        <priority>0.8</priority>
    </url>
    {% endfor %}

    <!-- Journals -->
    {% for journal in journals %}
    <url>
        <loc>{{ protocol }}://{{ domain }}/journals/{{ journal.url_slug }}/</loc>
        <lastmod>{{ journal.updated_at|date:"Y-m-d" }}</lastmod>
        <changefreq>weekly</changefreq>
        <priority>0.7</priority>
    </url>
    {% endfor %}

    <!-- Issues -->
    {% if issues %}
    {% for issue in issues %}
    <url>
        <loc>{{ protocol }}://{{ domain }}/journals/{{ issue.journal.url_slug }}/issues/{{ issue.id }}/</loc>
        <lastmod>{{ issue.updated_at|date:"Y-m-d" }}</lastmod>
        <changefreq>monthly</changefreq>
        <priority>0.75</priority>
    </url>
    {% endfor %}
    {% endif %}

    <!-- Authors -->
    {% for author in authors %}
    <url>
        <loc>{{ protocol }}://{{ domain }}/authors/{{ author.id }}/</loc>
        <lastmod>{{ author.updated_at|date:"Y-m-d" }}</lastmod>
        <changefreq>monthly</changefreq>
        <priority>0.6</priority>
    </url>
    {% endfor %}

</urlset>
